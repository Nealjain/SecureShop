{
  "name": "secure-ecommerce-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:convex\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python3 -m uvicorn main:app --reload --port 8000",
    "dev:convex": "cd convex && npx convex dev",
    "install:all": "npm install && (cd frontend && npm install) && (cd convex && npm install) && (cd backend && pip3 install -r requirements.txt)"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
